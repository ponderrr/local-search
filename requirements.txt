# Core Dependencies
python-dotenv==1.0.0
pandas==2.1.4

# Google Maps API
googlemaps==4.10.0

# Web Scraping & Verification
playwright==1.40.0

# Optional but recommended for better performance
asyncio-throttle==1.0.2  # For rate limiting async requests

# Data processing (if needed for future scripts)
openpyxl==3.1.2  # For Excel file support with pandas
numpy==1.26.2  # Often needed with pandas